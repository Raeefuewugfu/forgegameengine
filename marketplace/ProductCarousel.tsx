import React from 'react';

// This component is no longer used in the new marketplace design.
// Kept for potential future use or reference.
export const ProductCarousel = () => null;
export type Product = any;
