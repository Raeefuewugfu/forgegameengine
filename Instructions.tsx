import React from 'react';

const Instructions: React.FC = () => {
    // This component is not currently used but is kept for potential future use.
    // An empty file can sometimes cause build issues.
    return null; 
};

export default Instructions;
